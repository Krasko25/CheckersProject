#pragma once

// подъем поля, общие константы из разных классов
class GameConstants {
public:
    static const int BOARD_SIZE = 8;
    static const int MIN_PLAYER_NAME_LENGTH = 2;
    static const int MAX_PLAYER_NAME_LENGTH = 50;
    static const char* DEFAULT_AVATAR_PATH;
};